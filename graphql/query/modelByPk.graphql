query ModelByPk($id: String!) {
  models_by_pk(id: $id) {
    id
    description
    title
    thumbnail_url
    model_url
    views
    created_at
    updated_at
    owner {
      id
      name
      profile_photo_url
      email
      updated_at
      created_at
    }
  }
}
