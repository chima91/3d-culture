query Models {
  models(order_by: { created_at: desc }) {
    id
    title
    description
    thumbnail_url
    model_url
    owner {
      id
      email
      name
      profile_photo_url
      updated_at
      created_at
    }
    views
    updated_at
    created_at
  }
}
